name: "01-building-blocks"
run-name: "Test run"

on:
  push:
  workflow_dispatch:

jobs:
  echo-hello:
    runs-on: ubuntu-latest

    steps:
      - name: "Say hello"
        run: echo "Hello world"

  echo-goodbye:
    runs-on: ubuntu-latest

    steps:
      - name: "Failed step"
        run: echo "good socre"

      - name: "Say goodbye"
        run: echo "Goodbye Ben"
