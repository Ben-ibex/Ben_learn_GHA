name: "01-building-blocks"
run-name: "Test run"

on:
  push:
  workflow_dispatch:

jobs:
  echo-hello:
    runs-on: ubuntu-latest

    steps:
      - name: "Say hello"
        run: echo "Hello world"

  echo-goodbye:
    runs-on: ubuntu-latest

    steps:
      - name: "Failed step"
        run: exit 1

      - name: "Say goodbye"
        run: echo "Goodbye Ben"
